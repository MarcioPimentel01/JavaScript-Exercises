// function = a section of reusable code.
            // Declare a code once, use it whenever you want.
            // Call the function to execute that code.

// function to sing happy birday

function happyBDay(personBirtday, age) { //everithing in between the brakets are the parameters, everything in between the the curly braces are the function itself.
   
    console.log(`Happy Birday to you`);
    console.log(`Happy Birday to you`);
    console.log(`Happy Birday dear ${personBirtday}`);
    console.log(`Happy Birday to you`);
    console.log(`You are ${age} years old`);
};

// calling/Invoking the function happyBDay
happyBDay(`Marcio Pimentel`, 47); // in between the brakets we have arguments that need to match with the function parameters - the () after the function
happyBDay(`Dirceu Pimentel`, 72);
happyBDay(`Vanderli Pimentel`, 70);
happyBDay(`Monica Pimentel`, 45);
happyBDay(`Diego Pimentel`, 19);
happyBDay(`Laura Pimentel`, 13);



function add () {
    let x = 10
    let y = 30
    x + y
    console.log(x, y)
}

function subtract (){

}

function multiply (){

}

function divide () {

}