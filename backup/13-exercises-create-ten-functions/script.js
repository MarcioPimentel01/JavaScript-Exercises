// 01 Create a function called isPrime that takes a number as input and returns true if it's a prime number, and false otherwise.

const isPrime = (number) => {
    if (number % 2 === 0 || number % 3 === 0 || number % 5 === 0 || number % 7 === 0 || number % 11 === 0 || number % 13 === 0) {
        console.log(`Not a prime number`)
    } else {
        console.log(`Prime number`)
    }
    // const result = number.toString().split("").map(Number);
    // console.log(result);
    // let arraySum = result.reduce((acumulator, splitNumber) => acumulator + splitNumber, 0);
    // if (arraySum > 9) {
    //     isPrime(arraySum)
    // } else if (arraySum <= 9) {
    //     console.log(arraySum)
    // } 
}

// 02 Write a function called getMax that takes an array of numbers as input and returns the largest number in the array.

const getMax = () => {

}

// 03Implement a function called removeDuplicates that takes an array as input and returns a new array with duplicate elements removed.

const removeDuplicates = () => {

}

// 04 Create a function called calculateArea that takes the radius of a circle as input and returns the area of the circle.
 
const calculateArea = () => {

}

// 05 Write a function called formatDate that takes a Date object as input and returns a string representing the date in the format "DD/MM/YYYY".

const formatDate = () => {

}

// 06 Implement a function called reverseWords that takes a sentence as input and returns the sentence with the words reversed.

const reverseWords = () => {

}

// 07 Create a function called sortByLength that takes an array of strings as input and returns the array sorted by the length of each string, from shortest to longest.

const sortByLength = () => {

}

// 08 Write a function called sortByLength02 that takes an array as input and returns an array containing only the elements that appear more than once.

const sortByLength02 = () => {

}

// 09 Implement a function called calculateInterest that takes the principal amount, interest rate, and time period (in years) as input and returns the total amount after interest.

const calculateInterest = () => {

}

// 10 Create a function called convertTemperature that takes a temperature in Celsius as input and returns the equivalent temperature in Fahrenheit.
const convertTemperature = () => {

}

