// function = a section of reusable code.
            // Declare a code once, use it whenever you want.
            // Call the function to execute that code.

// function return key word ODD AND EVEN

function isEven(number) {
    // don't forget to have the argument metching with the parameter
    if(number % 2 === 0) {
        return true;
    } else false;
};

// return numenber % 2 === 0 ? true : false;
let result = isEven(10)
console.log(result);



